/**
 * Havstund Brain — HTTP-server (utvides i Steg D).
 * Foreløpig minimal placeholder slik at index.ts typer rent fra Steg A.
 */
import type { Config } from '../config.js';

export async function startServer(_config: Config): Promise<void> {
  throw new Error('startServer er ikke implementert ennå (Steg D)');
}
